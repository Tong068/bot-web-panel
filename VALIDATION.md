# 验证记录

验证日期：2026-09-08。测试账号与真实框架数据分开存储，测试不向真实群或好友发送消息。

## 已验证

- 后端测试：62 项，通过插件目录的 `npm test` 执行。
- 账号及群/私聊 ID 隔离、官机字符串 ID、新连接、离线与能力限制。
- 自动回复、Promise 返回对象保持、重复上报、多段回执、部分失败、超时与迟到回执、引用、@、文件与撤回。
- WAL 数据库重开、保留期清理、未读标记、SSE 补同步、重复加载和监听恢复。
- 登录、会话过期、CSRF、同源校验、限速、密码修改、上传大小、跨账号附件、路径及内网地址限制、媒体容量与卸载取消。
- 前端 `vue-tsc --noEmit` 和 Vite 生产构建通过。
- Playwright 使用框架 `serverLoad()` 在 2536 端口承载测试桩，验证快速切换账号、旧请求取消、独立草稿、批量附件归属、后台未读、断线恢复、图片预览、表情、引用、@、撤回及官机发送。
- 1440×960 桌面和 390×844 手机截图，浅色/深色主题；无页面异常、图片缺失或横向溢出，消息区与输入区无重叠。主要区域与 Stapxs 上游 `README/view.png` 对照。
- 后端端口测试拦截 `Server.prototype.listen`，确认插件未调用监听方法。插件生产代码不创建服务器。

## 本批新增验证

- 群、好友、机器人和成员头像按机器人及会话类型隔离；适配器异步头像、并发查找去重、失败不影响记录、旧消息补全及旧会话过期头像登记恢复。
- 置顶、已读/未读持久化，跨机器人以及群/私聊同 ID 隔离；刷新页面后恢复，菜单不触发真实发送。
- 浏览器验证完整复制超过 300 字的消息、选区复制、复制字符串 ID、@发送者、受鉴权图片下载、头像预览。
- 自动深色随系统变化、六种原主题色、消息对齐、会话显示、图片手动加载、Ctrl+Enter 发送和设置刷新后保留。发送键实现同时跳过输入法组合事件。
- 会话菜单的键盘导航、手机长按、移动取消；消息菜单右下角定位保留 8px 间距，缺少鼠标坐标的事件有回退位置。
- 设置页色块保持固定尺寸，1440×960、390×844 与 320×844 布局检查通过；新增截图包括 `settings-desktop-dark.png`、`settings-mobile.png`、`conversation-menu-mobile.png`、`message-menu-mobile.png`、`avatars-mobile-dark.png` 和 `settings-mobile-view-320.png`。截图仅保存在忽略跟踪的 `tests/output/`。

完整 Playwright 集成测试通过，页面异常列表为空。类型检查和最终生产构建已通过，最新产物位于 `web/dist`。

## 真实环境验证范围

最新消息与媒体回归包含 ICQQ / OneBot 的合并转发读取、嵌套媒体、接收引用和 @ 名称，以及 LLOneBot 的 `private_key` / `group_key`、NapCat rkey 格式、视频/语音下载链接刷新、历史文件标识恢复、SQLite 字段迁移和账号隔离。这些适配器接口测试使用本地测试桩，未对真实平台发送消息；不代表全部历史媒体已在真实账号上逐条下载验证。

真实框架检查页面、鉴权、机器人枚举、联系人及头像、`/status` 与 `/guoba/` 共用路由。聊天发送使用测试桩；真实平台的回复窗口、权限、文件及撤回限制由适配器和平台决定。

最终冒烟检查通过：`http://localhost:2536/bot-web/` 返回 200，未登录接口返回 401，登录和退出成功；共枚举到 OneBotv11、标准输入、锅巴沙盒三个在线实例，OneBot 连接在框架重载后正常恢复。Guoba、框架状态页和原始主题资源均返回 200。测试未向真实账号发送消息，真实框架保持运行。

本次读取 382 个联系人，其中 380 个返回鉴权头像路径；抽查 3 张头像，未登录访问均返回 401，登录后均返回有效图片，共 685922 字节。此结果证明已安装 OneBot 的真实头像链路可用，不代表所有平台或全部头像均可下载；未提供头像的实例/联系人使用回退图标。

Stapxs 主提交、Border-Card-UI 和 QFace 提交与 `UPSTREAM.md` 一致；`view.css`、`chat.css`、`msg.css`、`options.css` 与指定本地上游文件的 SHA-256 校验一致。

默认未开放的媒体内网地址需要在 `mediaHosts` 中明确配置。绕过标准适配器入口直接使用私有 SDK 或 HTTP 接口发送的第三方插件，不在当前发送监听范围内。

## 表情与 @ 面板修复验证

2026-09-08 使用 `node plugins/bot-web-panel/tests/composer-panels.mjs` 完成针对性回归。旧版本可复现表情面板距离工具栏 232/312px、320px 窄屏顶部越界，以及 @ 列表多人横向排列并溢出；修复后 8 个面板布局均在视口内，与工具栏保持 8px 间距，无横向溢出。

- 视口：1440×960、390×844、320×568、390×360；检查明暗主题和表情图片实际加载，截图为 `tests/output/composer-*.png`。
- 140 名成员完整滚动及末尾成员搜索，表情按名称/ID 搜索，空结果显示，选择后输入框获得焦点。
- 键盘方向键/Enter、Escape 恢复触发按钮焦点、关闭按钮和点击外部收起。
- 补充浅色/深色鼠标悬停测试：列表容器背景始终保持透明，仅当前成员行高亮；鼠标切换行及移出后背景正确恢复。修复前断言失败，修复后通过。截图为 `composer-members-hover-light.png`、`composer-members-hover-dark.png`。
- 加载时不显示上次列表；请求失败可重试；关闭或切换账号取消请求，测试观测到 2 次请求取消；过期响应不能覆盖新列表。
- 两个机器人分别通过测试桩发送，验证表情 ID 和成员字符串 ID 的归属；重复选择同一成员不重复添加 @；无成员列表能力时允许手动 ID，不支持 @ 时禁用按钮。

该脚本复用现有服务提供静态资源，并拦截全部面板 API；没有停止真实框架、创建新端口或发送真实聊天消息。页面异常列表为空。前端类型检查与生产构建通过，真实框架 `live-smoke.mjs` 检查也通过，三个机器人实例仍在线。此轮仅修改前端，后端 24 项测试结果沿用上一批记录。
