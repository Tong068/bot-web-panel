# Stapxs 功能对照与迭代清单

更新时间：2026-09-08。对照版本：Stapxs QQ Lite `7a895b964a67faf72f912370eb769adce16e37cc`。

这份清单按可验收功能分组，不把上游每一个开关或平台私有 API 算成独立功能。状态为「完成」「部分」「待办」「进行中」「不适用」；只有代码、构建和对应验证完成后才更新为完成。「完成」针对本表的验收范围，不表示所有适配器都能提供平台能力。

当前共 40 组：**完成 20、部分完成 7、待办 10、不适用 3**。适用于本面板的 37 组中，还有 **17 组未完成**（含 7 组部分实现）。

## 第一批工作

- [x] 群、好友、消息发送者头像：适配器头像接口、受鉴权缓存、历史补全、失败回退。
- [x] Stapxs 设置：自动深色、原主题色、发送键、独立显示消息、图片自动加载、会话显示方式。
- [x] 会话菜单：置顶/取消置顶、标记已读/未读、复制 ID、头像预览；按机器人与会话保存。
- [x] 消息菜单：完整文本复制、选中文本复制、图片下载、@发送者、复制消息 ID；键盘及手机长按。
- [x] 回归测试、桌面/手机截图、真实框架加载与只读检查。

## 功能对照

「来源」路径均相对于 Stapxs 的 `src/renderer/src/`。本插件继续使用上游原始 CSS、资源和组件结构，适配代码保留来源注释。

| ID | 功能 | 状态 | 来源 | 差距或验收标准 |
| --- | --- | --- | --- | --- |
| 01 | 群与好友头像 | 完成 | `FriendBody.vue`、联系人状态 | 读取适配器头像方法，列表与聊天标题一致；平台缺失时回退 |
| 02 | 消息发送者头像 | 完成 | `MsgBody.vue` | 群成员、私聊对方和本人独立解析，旧记录读取时补全 |
| 03 | 头像预览与回退 | 完成 | `ViewerCom.vue`、用户资料 | 受鉴权代理、过期登记恢复、加载失败回退，点击/菜单预览 |
| 04 | 深色、浅色、跟随系统 | 完成 | `options/OptView.vue` | 跟随系统变化，浏览器本地保存偏好，手动切换退出自动模式 |
| 05 | 原主题色选择 | 完成 | `options/OptView.vue` | 复用 Border-Card-UI 六种主题色，固定尺寸色块 |
| 06 | 背景图、透明、模糊 | 待办 | `options/OptView.vue` | 图片配置、可读性和移动端性能仍待评估 |
| 07 | 消息独立显示 | 完成 | `options/OptView.vue` | 自己的消息靠右/统一靠左 |
| 08 | 发送键配置 | 完成 | `options/OptFunction.vue` | 支持上游 Enter、Shift/Ctrl/Alt/Meta+Enter，跳过输入法组合事件 |
| 09 | 图片自动加载开关 | 完成 | `options/OptFunction.vue` | 关闭后点击占位才请求图片 |
| 10 | 最近/全部会话显示 | 完成 | `options/OptFunction.vue` | 「消息」页可切换最近会话与全部联系人 |
| 11 | 会话置顶 | 完成 | `pages/Messages.vue` | 置顶状态持久化，组内按最新消息排序，机器人及群/私聊隔离 |
| 12 | 手动已读/未读 | 完成 | `pages/Messages.vue` | 列表菜单操作；当前会话标为未读后回到列表 |
| 13 | 隐藏会话与群收纳盒 | 待办 | `pages/Messages.vue`、`OptFunction.vue` | 不删除历史，来新消息后的恢复规则需实现 |
| 14 | 通知、单会话静音 | 待办 | `options/OptFunction.vue` | 浏览器权限、前后台策略及跨标签页去重 |
| 15 | 完整/选中文本复制 | 完成 | `pages/Chat.vue` | 复制实际消息段而非 300 字摘要，支持选区与剪贴板回退 |
| 16 | 消息引用与撤回 | 完成 | `pages/Chat.vue` | 现有能力检测与适配器调用已测试 |
| 17 | 图片/附件下载菜单 | 完成 | `pages/Chat.vue` | 菜单项按实际附件出现，通过鉴权下载 |
| 18 | @发送者、复制 ID | 完成 | `pages/Chat.vue` | 会话目标、发送者、消息 ID 不做数字转换 |
| 19 | 菜单键盘/长按/边界定位 | 完成 | 消息/会话菜单 | Escape、方向键、Home/End、Shift+F10、手机长按；保留 8px 边界 |
| 20 | 复制图片到剪贴板 | 待办 | `pages/Chat.vue` | ClipboardItem、HTTPS 限制和格式兼容 |
| 21 | 撤回后重新编辑 | 待办 | `pages/Chat.vue` | 草稿恢复、附件仍有效和避免覆盖未发草稿 |
| 22 | 消息多选与转发 | 待办 | `MergePan.vue`、`pages/Chat.vue` | 多账号目标选择、普通/合并转发与能力限制 |
| 23 | 图片预览、缩放、切图 | 完成 | `ViewerCom.vue` | 现有预览组件已验证桌面与手机 |
| 24 | 最近表情、动画/超级表情 | 部分 | `FacePan.vue`、`EmojiFace.vue` | 已有 QFace 静态表情与搜索，缺最近与动态资源 |
| 25 | JSON/XML 卡片、Markdown | 部分 | `msg-component/`、`MsgBody.vue` | 当前为占位/纯文本，待复用结构化渲染并限制链接 |
| 26 | 合并转发完整浏览 | 部分 | `MergePan.vue` | 当前只展开已有文本摘要，缺远程节点加载 |
| 27 | 全库历史搜索、按日期定位 | 部分 | `History.vue`、`GlobalSessionSearchBar.vue` | 已有分页和已加载内容搜索，缺 SQLite 全库检索 |
| 28 | 好友分组与联系人资料 | 部分 | `pages/Friends.vue`、`UserInfoTooltip.vue` | 已有群/好友筛选，缺分组、资料面板与备注 |
| 29 | 群成员、公告、群文件 | 部分 | `DepPan.vue`、`BulletinBody.vue`、`FileManager.vue` | 已有 @ 成员查询，缺独立浏览与文件管理 |
| 30 | 消息表情回应、戳一戳 | 待办 | `pages/Chat.vue` | 需要按适配器新增白名单动作与结果处理 |
| 31 | 语音/视频播放与录制 | 部分 | `VoiceMsg.vue`、`MsgBody.vue` | 可播放已登记媒体，缺录制、波形与专用控件 |
| 32 | 文字、图片、文件输入 | 完成 | `pages/Chat.vue` | 选择、粘贴、拖拽，附件账号隔离已测试 |
| 33 | 草稿、未读与重连 | 完成 | 聊天与联系人状态 | 按机器人、会话类型和目标隔离，已验证迟到响应 |
| 34 | 持久历史、容量与保留期 | 完成 | `options/OptFunction.vue` | 由服务端 SQLite 与媒体缓存统一负责 |
| 35 | 设置导入导出与恢复默认 | 待办 | 设置状态 | 配置版本、敏感项排除与兼容验证 |
| 36 | 多语言 | 待办 | `options/OptView.vue`、语言包 | 当前仅简体中文 |
| 37 | 动画、缩放与更多布局主题 | 待办 | `options/OptView.vue` | 当前保留基础响应式与系统减少动画支持 |
| 38 | 机器人登录、连接管理 | 不适用 | `options/OptAccount.vue` | 交由 Yunzai 与适配器配置管理，不创建额外连接 |
| 39 | 桌面窗口、托盘、系统主题色 | 不适用 | Electron/Capacitor 后端 | 当前是框架提供的 Web 页面，无本机窗口权限 |
| 40 | 空间、遥测、上游应用更新 | 不适用 | Qzone、GA、Updater | 不属于多机器人聊天面板；未引入遥测 |

## 后续顺序

1. 第二批：全库历史搜索与日期定位、联系人资料、最近表情、撤回后重新编辑。
2. 第三批：消息卡片与合并转发、多选转发、群公告/文件浏览。
3. 第四批：通知与静音、背景与布局设置、配置导入导出；逐个适配器补齐可支持动作。

本清单用于后续逐项迭代，不代表剩余功能已经实现。每批在此记录变更、验证结果和仍受适配器限制的项目。

## 迭代记录

### 2026-09-08：第一批

已完成本批 15 组功能，新增设置页、通用菜单、头像解析及浏览器偏好存储。继续使用原始 CSS、QFace、Border-Card-UI 和 Stapxs 组件结构；原始设置页面一并保存供对照。

24 项后端测试通过；前端类型检查及生产构建通过；Playwright 桌面/手机与明暗主题通过，验证头像、置顶/未读隔离、完整复制、下载、设置持久化、组合键和菜单边界，无页面异常。设置页另检查 320px 窄屏布局。截图在 `tests/output/`，详细记录见 `VALIDATION.md`。

真实框架已恢复运行，访问地址：`http://localhost:2536/bot-web/`。只读检查枚举到 3 个在线机器人、382 个联系人，380 个联系人提供可登记头像，抽查 3 张头像均通过鉴权下载。Guoba 与状态页正常；未发送真实聊天消息。

### 2026-09-08：表情与 @ 面板修复

修复表情面板继承上游 `translateY(-100%)` 和移动端固定高度造成的位置偏移、出屏，以及 @ 面板继承 `.at-tag > div` 横向布局造成的多人挤压。保留 Stapxs 原始 CSS，通过限定范围的覆盖样式和共用弹层定位，保证面板在工具栏上方、列表独立滚动。

补齐点击外部/关闭按钮/Escape 收起、选择后聚焦输入框、表情空结果、成员加载/失败/重试、手动字符串 ID、重复 @ 去重及键盘选择。成员列表不再截断到前 100 人；关闭、重新打开或切换机器人时取消旧查询并丢弃过期结果。

新增 `tests/composer-panels.mjs`，复用运行中框架的静态页面，在浏览器中拦截全部 API 使用测试数据，无需停止框架或新增端口。验证 140 人列表、两个机器人、8 个面板布局（桌面、手机、320px 窄屏和低高度窗口）、明暗主题及失败/迟到响应；两次发送均为本地测试桩。类型检查、生产构建和真实框架只读检查通过。上表总数不变，本次修复已有功能。

后续修正成员列表悬停：上游 `.at-tag > div:hover` 原本会将整个列表容器变色，现提高列表透明背景规则的选择器优先级，仅保留成员按钮自身高亮。新增浅色/深色悬停断言，验证容器和其他行背景不变、切换行后高亮转移、移出后恢复；修复前测试可复现失败，修复后全部通过，生产产物已更新。
